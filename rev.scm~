(define (sqrt x)
  (define (try guess)
    (if (good-enough? guess)
	guess
	(try (avg guess (/ x guess)))
	))
  (define (good-enough? guess)
    (< (abs (- (square guess) x)) 0.0001))
  (define (avg a b)
    (/ (+ a b) 2))
  (try 1))

(sqrt 4)
